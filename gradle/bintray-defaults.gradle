bintray {
    user = bintrayUser
    key = bintrayKey

    publications = ['main']

    dryRun = false
    publish = false

    pkg {
        name = "${group}:${project.name}"
        desc = "$description"

        publicDownloadNumbers = true
    }
}

